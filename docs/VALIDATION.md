# 검증 결과 · 2026-09-07

## 자동 검사

- Vitest: 6개 파일, **28개 테스트 통과**.
- TypeScript: `npm run typecheck` 통과, 오류 0건.
- `npm run build`: 성공. 정적 JS/CSS/6종 SDF 및 오프라인 service worker 생성.
- `node scripts/check-build.mjs`: 상대경로, 6종 SDF 존재, 오프라인 필수 파일 목록 검사 통과.
- RDKit 2026.03.6: 6종의 모든 26개 입체중심을 3D에서 판정하여 CCD와 일치 확인. 정렬 후 SDF round-trip에서도 일치.
- 실제 3Dmol 2.5.5 SDF 파서: 6종의 원자 순서, 명시적 H, 좌표, 원자가/결합수 유지 확인.

## 테스트가 확인한 동작

| 영역     | 확인 내용                                                                                |
| -------- | ---------------------------------------------------------------------------------------- |
| 데이터   | 파일 SHA-256, 분자식, 원자가, 탄소 번호, OH 원자, 아노머 탄소의 두 O 이웃                |
| 입체화학 | C1 anomer, C4 epimer, C2 deoxy, ⁴C₁ 기하, glucose 고리 정렬                              |
| 주석     | fructose C2 강조, α/β 치환기 라벨, OH/고리 O 구별, H 숨김, 공간채움, Haworth 선택        |
| 비교     | 양방향 카메라 전달, 순환 호출 방지, OFF 후 연결 해제                                     |
| 로드     | 정적 경로, 초기 회전 OFF, 정리, 실패/재시도, 전환 시 WebGL 뷰어 재사용, 오래된 응답 무시 |
| 수업 UI  | 해설 기본 숨김/펼침/접기, Haworth click/keyboard 동작                                    |

## 검증의 한계

3Dmol 파서 테스트는 실제 라이브러리를 사용합니다. 렌더러 수명주기/주석 테스트는 mock을, 수업 UI 테스트는 jsdom을 사용합니다. 실제 GPU의 WebGL 렌더링, 마우스/터치 제스처, 200% 화면 확대 및 프로젝터·태블릿에서의 라벨 겹침은 실기기 검증이 남아 있습니다. 오프라인 파일 목록을 검사했지만 실제 브라우저의 설치·업데이트·연결 차단은 검증하지 않았습니다.

WebMCP는 미지원 브라우저에서 조용히 생략합니다. 이 환경에는 실제 WebMCP 도구 실행을 검증할 수 있는 연결이 없어 해당 계약의 실환경 실행은 확인하지 않았습니다.

빌드 중 3Dmol의 callback 문자열 평가 기능에서 direct-eval 경고 1건이 발생합니다. 앱은 해당 문자열 callback 기능을 호출하지 않으며 빌드는 성공합니다.

## 수업 전 실기기 점검 제안

1. β 포도당이 정상 렌더링되고 회전·확대·초기화가 되는지 확인합니다.
2. α/β 비교와 회전 동기화 ON/OFF를 양쪽에서 확인합니다.
3. 6종을 전환하고 H/탄소 번호/OH/아노머/axial 표시가 읽히는지 확인합니다.
4. 태블릿 두 손가락 확대와 화면 회전을 확인합니다.
5. 배포본의 ‘오프라인 사용 준비 완료’ 후 연결을 끊고 새로고침 및 6종 전환을 확인합니다.
